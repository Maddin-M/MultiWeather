pluginManagement {
    plugins {
        id "com.github.johnrengelman.shadow" version "$shadowVersion"
        id "name.remal.sonarlint" version "$sonarlintVersion"
        id "com.github.spotbugs" version "$spotbugsVersion"
    }
}

rootProject.name = "MultiWeather"